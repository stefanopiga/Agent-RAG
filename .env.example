# Database Configuration SUPABASE
# PostgreSQL with PGVector extension
# Connection string includes credentials (user/password) - no separate API keys needed
DATABASE_URL="postgresql://postgres.[user]:[password]@aws-1-eu-north-1.pooler.supabase.com:5432/postgres"

# Note: Epic 3 uses direct PostgreSQL connection via DATABASE_URL (AsyncPG)
# Supabase API keys (ANON_KEY, SERVICE_ROLE_KEY) are NOT needed for Epic 3
# If you need Supabase client Python in future, add:
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key (backend only, never expose)





# OpenAI Configuration (Required)
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# Model Selection
# Choose which OpenAI model to use for both ingestion and agent
LLM_CHOICE=gpt-4o-mini

# Embedding Model
EMBEDDING_MODEL=text-embedding-3-small

# Development Settings
LOG_LEVEL=INFO
DEBUG_MODE=false

# LangFuse Configuration (Optional)
# For observability and tracing of MCP server operations
# Get from https://cloud.langfuse.com or self-hosted instance
LANGFUSE_PUBLIC_KEY=your-langfuse-public-api-key
LANGFUSE_SECRET_KEY=your-langfuse-secret-api-key
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# Epic 3: Cost Protection (Recommended for private use)
# Prevents cost explosion from external attacks
COST_DAILY_LIMIT=10.00      # Daily cost limit in USD (default: $10/day)
COST_HOURLY_LIMIT=2.00      # Hourly cost limit in USD (default: $2/hour)
COST_ALERT_THRESHOLD=5.00   # Alert threshold in USD (default: $5/day)

# Epic 3: Streamlit Authentication (Optional)
# Simple password protection if network security not sufficient
# Generate hash: python -c "import hashlib; print(hashlib.sha256('your-password'.encode()).hexdigest())"
# STREAMLIT_PASSWORD_HASH=your-password-sha256-hash

# Epic 3: Rate Limiting (Optional)
# Redis URL for persistent rate limiting (if using Redis-based rate limiter)
# REDIS_URL=redis://localhost:6379